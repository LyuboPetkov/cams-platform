# Resolved by installing fastapi==0.141.1, uvicorn[standard]==0.52.3 and
# sentence-transformers==5.7.0, then `pip freeze` — pinned in full for a
# reproducible thesis artifact rather than left to float. Resolved on Python
# 3.12 on Windows; the container's python:3.13-slim + Linux wheels for every
# package here were verified to exist before pinning (Section 4 of the
# Phase 16 brief). uvloop is intentionally absent: it has no Windows wheel so
# it never appeared in the freeze, and uvicorn runs correctly without it —
# just without that optional event-loop swap.
#
# torch below is still pinned here for documentation, but the Dockerfile
# installs it separately first from PyTorch's CPU wheel index — see the
# comment there for why (the default PyPI Linux wheel drags in ~2GB of
# nvidia-cu13/cuda-toolkit packages this GPU-less container never uses).
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.4.2
fastapi==0.141.1
filelock==3.32.3
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
huggingface_hub==1.27.0
idna==3.18
Jinja2==3.1.6
joblib==1.5.3
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
narwhals==2.24.0
networkx==3.6.1
numpy==2.5.2
packaging==26.3
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
python-dotenv==1.2.3
PyYAML==6.0.3
regex==2026.7.19
rich==15.0.0
safetensors==0.8.0
scikit-learn==1.9.0
scipy==1.18.0
sentence-transformers==5.7.0
setuptools==84.0.0
shellingham==1.5.4
starlette==1.6.0
sympy==1.14.0
threadpoolctl==3.6.0
tokenizers==0.22.2
torch==2.13.0
tqdm==4.70.0
transformers==5.15.0
typer==0.27.1
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.3
watchfiles==1.2.0
websockets==17.0.1
