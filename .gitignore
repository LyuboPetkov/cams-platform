# Maven compiled output
target/

# Node / Vite
frontend/node_modules/
frontend/dist/

# IntelliJ IDEA
.idea/
*.iml

# macOS
.DS_Store

# Spring Boot — do not commit if it contains plaintext secrets

.env

# Claude Code project instructions (local-only)
CLAUDE.md

#Phase docs, meant for local use only
docs/

# ESCO source dataset — only needed to regenerate V10; the migration itself
# is the artifact of record, so the 9MB CSV stays out of the repo
esco-v1.2.1-en/

# Phase 18 matching-eval scripts — local virtualenv and bytecode cache
scripts/matching-eval/.venv/
scripts/matching-eval/__pycache__/